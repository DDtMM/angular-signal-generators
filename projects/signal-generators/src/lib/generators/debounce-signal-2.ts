import { Signal, signal, effect, WritableSignal } from '@angular/core';
import { ComputationOrSignal, toSignal } from '../internal/signal-like';

// export function debounceSignal2<T>(src: ComputationOrSignal<T>, dueTime: number | ComputationOrSignal<number>, initialValue: T): Signal<T> {
//   timer
// }

